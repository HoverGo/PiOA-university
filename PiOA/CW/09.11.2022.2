from random import *


# 23.Дан двухмерныймассив, состоящий из N, Mцелочисленных элементов.
# 23.1.Вычислить сумму положительных элементов двухмерного массива, кратных
# 3.23.2.Для  элементов  массива,  находящихся  в  четных  строках  и  нечетных  столбцах вычислить сумму
# положительных элементов массива, кратных 5 и вывести все элементы массива больше найденной суммы на экран и их индексы.
def init_array():
    n = int(input("Введите количество строк: "))
    m = int(input("Введите количество столбцов: "))
    spisok = [[randint(-50, 50) for i in range(m)] for j in range(n)]
    print("Список создан!")
    print(spisok)
    return spisok


def print_1(mass):
    print("Список:")
    print(mass)


def vibor(mass):
    i = int(input("Введите индекс строки для изменения элемента: "))
    j = int(input("Введите индекс столбца для изменения элемента: "))
    mass[i][j] = float(input("Введите элемент, на который хотите заменить: "))
    return mass


def summa(mass):
    summ = 0
    for i in range(len(mass)):
        for j in range(len(mass[0])):
            if mass[i][j] > 0 and abs(mass[i][j]) % 3 == 0:
                summ += mass[i][j]
    print("Сумма положительных элементов массива, кратных 3 =", summ)


def summachetnechet(mass):
    summ = 0
    elem = []
    for i in range(0, len(mass), 2):
        for j in range(1, len(mass[0]), 2):
            if mass[i][j] > 0 and abs(mass[i][j]) % 5 == 0:
                summ += mass[i][j]
    print("Сумма положительных элементов массива в чётных строках и нечётных столбцах, кратных 5 =", summ)
    print("Элементы массива и их индексы, большие суммы выше(индексы указаны в скобках): ", end="")
    for k in range(len(mass)):
        for p in range(len(mass[0])):
            if mass[k][p] > summ:
                print(f", {mass[k][p]}({k},{p})", end="")
    print("\n")


def main():
    spisok = init_array()
    vibor(spisok)
    print_1(spisok)
    summa(spisok)
    summachetnechet(spisok)


main()
